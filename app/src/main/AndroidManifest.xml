<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rosstox26.pathos.projectpathos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EarnCoins" />
        <activity android:name=".AccountSetup" />
        <activity android:name=".Goals" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".RedeemRewards" />
        <activity
            android:name=".RedemtionCode"
            android:label="@string/title_activity_redemtion_code"></activity>
    </application>

</manifest>