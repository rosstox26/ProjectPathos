<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rosstox26.pathos.projectpathos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <uses-permission android:name="android.permission.INTERNET"/> <!--added this line as a part of the Facebook login functionality -->
        <meta-data android:name="com.facebook.sdk.ApplicationID"
            android:value="@string/facebook_app_id" /> <!--added this line as a part of the Facebook login functionality -->
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".EarnCoins" />
        <activity android:name=".AccountSetup" />
        <activity android:name=".Goals" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".RedeemRewards"></activity>
    </application>

</manifest>